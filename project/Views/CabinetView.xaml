<UserControl x:Class="project.Views.CabinetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:converters="clr-namespace:project.Converters"
             xmlns:services="clr-namespace:project.Services"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000"
             Background="#131314">
    <UserControl.Resources>
        <converters:LocalizedMaterialConverter x:Key="LocalizedMaterialConverter" />
        <converters:LanguageStringConverter x:Key="LanguageStringConverter" />
    </UserControl.Resources>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,30">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{Binding Source={x:Static services:TranslationSource.Instance}, Path=[Cabinet]}" FontSize="24" FontWeight="Bold" Foreground="#FFFFFF"/>
                <TextBlock Text="{Binding Username, StringFormat=': {0}'}" FontSize="24" FontWeight="Bold" Foreground="#FFFFFF"/>
            </StackPanel>
        </Grid>

        <Border Grid.Row="1" Background="#1F1F20" CornerRadius="8" Padding="20" BorderBrush="#2A2A2B" BorderThickness="1">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="{Binding Source={x:Static services:TranslationSource.Instance}, Path=[MyOrders]}" FontSize="18" FontWeight="Bold" Foreground="#FFFFFF" Margin="0,0,0,20"/>
                    
                    <DataGrid ItemsSource="{Binding MyOrders}" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single" RowHeight="40" FontSize="14" 
                              Background="#131314" Foreground="#E0E0E0" HeadersVisibility="Column" BorderBrush="#444444" BorderThickness="1" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#2A2A2B">
                        <DataGrid.Resources>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="#1F1F20"/>
                                <Setter Property="Foreground" Value="#999999"/>
                                <Setter Property="Padding" Value="10"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                            </Style>
                            <Style TargetType="DataGridRow">
                                <Setter Property="Background" Value="#131314"/>
                                <Style.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="#2A2A2B"/>
                                        <Setter Property="Foreground" Value="#FFB3B1"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.Resources>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="Auto"/>
                            <DataGridTextColumn Header="{Binding Source={x:Static services:TranslationSource.Instance}, Path=[OrderDate]}" Binding="{Binding OrderDate, StringFormat='{}{0:dd.MM.yyyy}'}" Width="Auto"/>
                            <DataGridTemplateColumn Header="{Binding Source={x:Static services:TranslationSource.Instance}, Path=[Name]}" Width="*">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock VerticalAlignment="Center">
                                            <TextBlock.Text>
                                                <MultiBinding Converter="{StaticResource LocalizedMaterialConverter}" ConverterParameter="OrderName">
                                                    <Binding Path="." />
                                                    <Binding Source="{x:Static services:TranslationSource.Instance}" Path="CurrentLanguage" />
                                                </MultiBinding>
                                            </TextBlock.Text>
                                        </TextBlock>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn Header="{Binding Source={x:Static services:TranslationSource.Instance}, Path=[Cost]}" Binding="{Binding TotalCost, StringFormat='{}{0} $'}" Width="Auto"/>
                            <DataGridTemplateColumn Header="{Binding Source={x:Static services:TranslationSource.Instance}, Path=[Status]}" Width="1*">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock>
                                            <TextBlock.Text>
                                                <MultiBinding Converter="{StaticResource LanguageStringConverter}">
                                                    <Binding Path="Status" />
                                                    <Binding Source="{x:Static services:TranslationSource.Instance}" Path="CurrentLanguage" />
                                                </MultiBinding>
                                            </TextBlock.Text>
                                        </TextBlock>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
