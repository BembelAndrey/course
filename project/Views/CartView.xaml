<UserControl x:Class="project.Views.CartView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:services="clr-namespace:project.Services"
             xmlns:converters="clr-namespace:project.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000"
             Background="#131314">
    <UserControl.Resources>
        <converters:LocalizedMaterialConverter x:Key="LocalizedMaterialConverter" />
    </UserControl.Resources>
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Text="{Binding Source={x:Static services:TranslationSource.Instance}, Path=[CartTitle]}" FontSize="28" FontWeight="Bold" Foreground="#FFFFFF" Margin="0,0,0,20"/>

        <!-- Список товаров в корзине -->
        <Border Grid.Row="1" Background="#1F1F20" CornerRadius="8" Padding="15" BorderBrush="#2A2A2B" BorderThickness="1" Margin="0,0,0,20">
            <DataGrid ItemsSource="{Binding Items}" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single"
                      Background="#131314" Foreground="#E0E0E0" HeadersVisibility="Column" BorderThickness="0" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#2A2A2B">
                <DataGrid.Resources>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#1F1F20"/>
                        <Setter Property="Foreground" Value="#999999"/>
                        <Setter Property="Padding" Value="10"/>
                    </Style>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Background" Value="#131314"/>
                    </Style>
                </DataGrid.Resources>
                <DataGrid.Columns>
                    <DataGridTemplateColumn Header="{Binding Source={x:Static services:TranslationSource.Instance}, Path=[Product]}" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock VerticalAlignment="Center">
                                    <TextBlock.Text>
                                        <MultiBinding Converter="{StaticResource LocalizedMaterialConverter}" ConverterParameter="Title">
                                            <Binding Path="Material" />
                                            <Binding Source="{x:Static services:TranslationSource.Instance}" Path="CurrentLanguage" />
                                        </MultiBinding>
                                    </TextBlock.Text>
                                </TextBlock>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="{Binding Source={x:Static services:TranslationSource.Instance}, Path=[Cost]}" Binding="{Binding Material.Price, StringFormat='{}{0} $'}" Width="Auto"/>
                    <DataGridTextColumn Header="{Binding Source={x:Static services:TranslationSource.Instance}, Path=[Qty]}" Binding="{Binding Quantity}" Width="Auto"/>
                    <DataGridTemplateColumn Width="Auto">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="X" Command="{Binding DataContext.RemoveFromCartCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" 
                                        CommandParameter="{Binding}" Background="Transparent" Foreground="#FFB3B1" BorderThickness="0" FontWeight="Bold" Cursor="Hand" Padding="10,5"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- Оформление заказа -->
        <Border Grid.Row="2" Background="#1F1F20" CornerRadius="8" Padding="20" BorderBrush="#2A2A2B" BorderThickness="1">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <StackPanel VerticalAlignment="Center" Margin="0,0,30,0">
                    <TextBlock Text="{Binding Source={x:Static services:TranslationSource.Instance}, Path=[Address]}" Foreground="#999999" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding Address, UpdateSourceTrigger=PropertyChanged}" Width="250" Height="30" VerticalContentAlignment="Center"/>
                </StackPanel>
                <StackPanel VerticalAlignment="Center" Margin="0,0,30,0">
                    <TextBlock Text="{Binding Source={x:Static services:TranslationSource.Instance}, Path=[TotalCostLbl]}" Foreground="#999999" Margin="0,0,0,5"/>
                    <TextBlock Text="{Binding TotalCost, StringFormat='{}{0} $'}" FontSize="24" FontWeight="Bold" Foreground="#FFB3B1"/>
                </StackPanel>
                
                <Button Content="{Binding Source={x:Static services:TranslationSource.Instance}, Path=[OrderServiceBtn]}" Command="{Binding CheckoutCommand}" Style="{StaticResource AccentButton}" Height="40" Padding="20,0"/>
                
                <TextBlock Text="{Binding Message}" Foreground="#FFB3B1" FontWeight="Bold" TextWrapping="Wrap" Margin="15,0,0,0" VerticalAlignment="Center" MaxWidth="200"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
