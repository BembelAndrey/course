<UserControl x:Class="project.Views.MaterialDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:services="clr-namespace:project.Services"
             xmlns:converters="clr-namespace:project.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000"
             Background="#131314">
    <UserControl.Resources>
        <converters:LocalizedMaterialConverter x:Key="LocalizedMaterialConverter" />
        <converters:LanguageStringConverter x:Key="LanguageStringConverter" />
        <converters:StringToVisibilityConverter x:Key="StringToVisibilityConverter" />
    </UserControl.Resources>
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Button Content="{Binding Source={x:Static services:TranslationSource.Instance}, Path=[BackToCat]}" Command="{Binding GoBackCommand}" HorizontalAlignment="Left" Padding="10,5" Background="Transparent" BorderThickness="0" Foreground="#999999" FontWeight="Bold" Cursor="Hand" Margin="0,0,0,20"/>

            <!-- Информация о товаре -->
            <Border Grid.Row="1" Background="#1F1F20" Padding="30" CornerRadius="8" BorderBrush="#2A2A2B" BorderThickness="1" Margin="0,0,0,30">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="300"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Height="300" Background="#131314" CornerRadius="8" Margin="0,0,30,0">
                        <Image Source="{Binding Material.ImagePath, Converter={StaticResource ImagePathConverter}}" Stretch="Uniform"/>
                    </Border>

                    <StackPanel Grid.Column="1">
                        <TextBlock FontSize="14" Foreground="#999999" Margin="0,0,0,5">
                            <TextBlock.Text>
                                <MultiBinding Converter="{StaticResource LocalizedMaterialConverter}" ConverterParameter="Category">
                                    <Binding Path="Material" />
                                    <Binding Source="{x:Static services:TranslationSource.Instance}" Path="CurrentLanguage" />
                                </MultiBinding>
                            </TextBlock.Text>
                        </TextBlock>
                        <TextBlock FontSize="28" FontWeight="Bold" Foreground="#FFFFFF" Margin="0,0,0,20" TextWrapping="Wrap">
                            <TextBlock.Text>
                                <MultiBinding Converter="{StaticResource LocalizedMaterialConverter}" ConverterParameter="Title">
                                    <Binding Path="Material" />
                                    <Binding Source="{x:Static services:TranslationSource.Instance}" Path="CurrentLanguage" />
                                </MultiBinding>
                            </TextBlock.Text>
                        </TextBlock>
                        <TextBlock FontSize="16" Foreground="#CCCCCC" TextWrapping="Wrap" Margin="0,0,0,25" LineHeight="24">
                            <TextBlock.Text>
                                <MultiBinding Converter="{StaticResource LocalizedMaterialConverter}" ConverterParameter="Description">
                                    <Binding Path="Material" />
                                    <Binding Source="{x:Static services:TranslationSource.Instance}" Path="CurrentLanguage" />
                                </MultiBinding>
                            </TextBlock.Text>
                        </TextBlock>
                        <TextBlock Text="{Binding Material.Price, StringFormat='Цена: {0} $'}" FontSize="24" FontWeight="Bold" Foreground="#FFB3B1" Margin="0,0,0,10"/>
                        
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Source={x:Static services:TranslationSource.Instance}, Path=[StockLbl]}" FontSize="16" Foreground="#999999" Margin="0,0,5,0"/>
                            <TextBlock Text="{Binding Material.StockQuantity}" FontSize="16" Foreground="#E0E0E0" FontWeight="Bold"/>
                        </StackPanel>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Отзывы -->
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="30"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>

                <!-- Список отзывов -->
                <Border Grid.Column="0" Background="#1F1F20" CornerRadius="8" Padding="20" BorderBrush="#2A2A2B" BorderThickness="1" MinHeight="400">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="{Binding Source={x:Static services:TranslationSource.Instance}, Path=[ReviewsTitle]}" FontSize="18" FontWeight="Bold" Foreground="#FFFFFF" Margin="0,0,0,15"/>
                        <ListBox Grid.Row="1" ItemsSource="{Binding Reviews}" BorderThickness="0" Background="Transparent" ScrollViewer.HorizontalScrollBarVisibility="Disabled" ScrollViewer.VerticalScrollBarVisibility="Auto" MaxHeight="600">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="#2A2A2B" BorderThickness="0,0,0,1" Padding="0,15" Margin="0,0,0,5" Width="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=ListBox}}">
                                        <StackPanel>
                                            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                                <TextBlock Text="{Binding User.Username}" FontWeight="Bold" Foreground="#FFB3B1" Margin="0,0,15,0"/>
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Foreground="#CCCCCC">
                                                        <TextBlock.Text>
                                                            <MultiBinding Converter="{StaticResource LanguageStringConverter}">
                                                                <Binding Source="RatingVal" />
                                                                <Binding Source="{x:Static services:TranslationSource.Instance}" Path="CurrentLanguage" />
                                                            </MultiBinding>
                                                        </TextBlock.Text>
                                                    </TextBlock>
                                                    <TextBlock Text="{Binding Rating, StringFormat='{}{0}/5'}" Foreground="#CCCCCC"/>
                                                </StackPanel>
                                            </StackPanel>
                                            <TextBlock Text="{Binding Text}" Foreground="#E0E0E0" TextWrapping="Wrap" LineHeight="20"/>
                                            <TextBlock Text="{Binding CreatedAt, StringFormat='{}{0:dd.MM.yyyy}'}" FontSize="12" Foreground="#666666" Margin="0,10,0,0"/>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Grid>
                </Border>

                <!-- Форма написания отзыва -->
                <Border Grid.Column="2" Background="#1F1F20" CornerRadius="8" Padding="20" BorderBrush="#2A2A2B" BorderThickness="1" VerticalAlignment="Top">
                    <StackPanel>
                        <TextBlock Text="{Binding Source={x:Static services:TranslationSource.Instance}, Path=[LeaveReview]}" FontSize="18" FontWeight="Bold" Foreground="#FFFFFF" Margin="0,0,0,20"/>
                        
                        <TextBlock Text="{Binding Source={x:Static services:TranslationSource.Instance}, Path=[RatingLbl]}" Foreground="#999999" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding NewReviewRatingText, UpdateSourceTrigger=PropertyChanged}" Width="60" HorizontalAlignment="Left" Margin="0,0,0,20" />
                        
                        <TextBlock Text="{Binding Source={x:Static services:TranslationSource.Instance}, Path=[ReviewTextLbl]}" Foreground="#999999" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding NewReviewText, UpdateSourceTrigger=PropertyChanged}" Height="120" TextWrapping="Wrap" AcceptsReturn="True" Margin="0,0,0,20" />
                        
                        <Button Content="{Binding Source={x:Static services:TranslationSource.Instance}, Path=[SendBtn]}" Command="{Binding SubmitReviewCommand}" Style="{StaticResource AccentButton}" Height="50" Margin="0,0,0,10" VerticalAlignment="Bottom"/>
                        
                        <TextBlock Text="{Binding Message}" Foreground="#FFB3B1" TextWrapping="Wrap" FontWeight="SemiBold"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
    </ScrollViewer>
</UserControl>
